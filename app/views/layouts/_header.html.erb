<nav>
  <p>
    <% if current_user %>
    <div style="margin-right: 100px;"">
      <%= link_to 'My Account', current_user,class: "btn btn-default btn-sm" %>
      <%= link_to 'Sign Out', session_path, method: :delete, class: "btn btn-default btn-sm" %>
        <span style="float:right;"> Welcome, <%= current_user.name %></span>
    </div>
    <% else %>
      <%= link_to 'Sign Up', signup_path, class:"btn btn-default btn-sm" %>
      <%= link_to 'Sign In', new_session_path, class:"btn btn-default btn-sm" %>
    <% end %>
  </p>
</nav>

<div class="row">

<div class="col-sm-2">
<h3>
<%= link_to "Home", root_path %>
</h3>
</div>

<div class="col-sm-2">
<h3>
<% if current_user %>
  <% if current_user.current_team == nil %>
    <%= link_to 'My Team', new_team_path %>
  <% else %>
    <%= link_to 'My Team', edit_team_path(current_user.current_team) %>
  <% end %>
<% else %>
    <%= link_to 'My Team', new_session_path %>
<% end %>
</h3>
</div>

<div class="col-sm-2">
<h3>
<%= link_to "Crimes", crimes_path %>
</h3>
</div>

<div class="col-sm-2">
<h3>
<%= link_to "Power Rankings", nflteams_path %>
</h3>
</div>

</div>
